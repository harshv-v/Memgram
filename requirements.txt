# SDK (what `pip install memgram` pulls)
openai>=1.0
httpx>=0.27
pydantic>=2.0
# Backend (API + worker)
fastapi>=0.111
uvicorn>=0.30
asyncpg>=0.29
redis>=5.0
pgvector>=0.3
python-jose>=3.3
# Dev / zero-infra testing
fakeredis>=2.20
